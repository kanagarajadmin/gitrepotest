name: Build OpenApi file
on: 
  push:
    branches: ["stage","prod"]
jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
        - name: 'Checkout GitHub Action'
          uses: actions/checkout@v3
 
        - name: 'echo'
          run: |
            echo "Hello world"
